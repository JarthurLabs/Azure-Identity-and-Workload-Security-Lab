// Confirm that blob reads used OAuth rather than Shared Key.
StorageBlobLogs
| where TimeGenerated > ago(24h)
| where AccountName startswith "stcarebridge"
| project
    TimeGenerated,
    OperationName,
    StatusCode,
    StatusText,
    AuthenticationType,
    TlsVersion
| order by TimeGenerated desc
